# from django.urls import path
# from .views import (OrgCreateView,
#                     OrgListView,
#                     OrgDetailView,
#                     )
# from event.views import EventCreateView
#
# app_name = "org"
#
# urlpatterns = [
#     path('', OrgListView.as_view(), name="orgs"),
#     path('create/', OrgCreateView.as_view(), name="create-org"),
#     path('<slug:slug>/', OrgDetailView.as_view(), name="org"),
#     path('<slug:slug>/create/', EventCreateView.as_view(), name="create-event"),
#
#
#
#
# ]
